# MongoDB Connection String - MongoDB Atlas free tier
# Replace with your actual MongoDB Atlas connection string when deploying
# For development, we're using a demo connection string that doesn't require authentication
MONGO_URI=mongodb+srv://demo:demo@cluster0.mongodb.net/prepengine?retryWrites=true&w=majority

# JWT Secret - Change this to a secure random string in production
JWT_SECRET=prepengine_secure_jwt_secret_key_change_this_in_production

# Server Port
PORT=3000

# CORS Allowed Origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000,http://localhost:5500,http://localhost:8080,http://127.0.0.1:5500,http://127.0.0.1:5000,http://127.0.0.1:3000
